import { EventScheduleModel } from './event-schedule.model.model';

describe('EventScheduleModel', () => {
  it('should create an instance', () => {
    expect(new EventScheduleModel()).toBeTruthy();
  });
});
